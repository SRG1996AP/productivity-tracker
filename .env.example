# Flask Configuration
FLASK_ENV=production
FLASK_APP=wsgi.py

# Security - Generate a strong random key
SECRET_KEY=your-super-secret-random-key-here

# Database URL (use PostgreSQL for production)
# Local SQLite (development): sqlite:///site.db
# PostgreSQL format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/productivity_tracker

# Server
HOST=0.0.0.0
PORT=5000
